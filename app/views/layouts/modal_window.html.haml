.modal.fade{:id => controller_name+'_'+action_name+'_modal', "aria-hidden" => "true", :role => "dialog", :tabindex => "-1", "data-backdrop" => "static"}
  .modal-dialog.card
    .modal-content
      .modal-header.card-header
        = yield :modal_header
      = yield
      = yield :modal_footer