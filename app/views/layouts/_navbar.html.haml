%nav.navbar.navbar-default.navbar-fixed-top{"role" => "navigation"}
  .container
    .navbar-header
      %button.navbar-toggle{"type" => "button", "data-target" => "#nav-collapse-01", "data-toggle" => "collapse"}
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      %a.navbar-brand.media{:href => root_url}
        .pull-left.navbar-logo-text
          e
        .pull-left.navbar-logo
          = image_tag('logo/ebentum_38.png', "class" => "media-object" )
        .media-body
          entum
    #nav-collapse-01.collapse.navbar-collapse
      .visible-md.visible-lg
        %ul.nav.navbar-nav.navbar-left
          %li{ :class => (controller_name == "activities" and action_name == "index") ? "active" : nil }
            %a{:href => root_url}
              %i.fa.fa-rss-square
              = t('.activity')
          %li{ :class => (controller_name == "events" and action_name == "search") ? "active" : nil }
            %a{:href => "/events/event_search"}
              %i.fa.fa-compass
              = t('.explore')

          %li{ :class => (controller_name == "users" and action_name == "search") ? "active" : nil }
            %a{:href => "/users/search"}
              %i.fa.fa-users
              = t('.people')

        - if user_signed_in?
          %ul.nav.navbar-nav.navbar-right
            %li
              %a.events_new{:href => "#"}
                %i.fa.fa-plus-square
                = t('share')
            %li.dropdown
              %a.dropdown-toggle{:href => "#", "data-toggle" => "dropdown"}
                %i.fa.fa-user
                = current_user.complete_name
              %ul.dropdown-menu
                %li
                  %a{:href => user_path(current_user)}
                    %i.fa.fa-user
                    = t('.profile')
                %li
                  %a{:href => edit_user_registration_path(current_user)}
                    %i.fa.fa-cog
                    = t('.configuration')
                %li.divider
                %li
                  %a{:href => destroy_user_session_path, :data => {:method => :delete}}
                    %i.fa.fa-power-off
                    = t('.logout')
        - else
          %ul.nav.navbar-nav.navbar-right
            %li
              = link_to t('.sign_up'), new_user_registration_path
            %li
              = link_to t('.sign_in'), new_user_session_path
      .visible-sm
        %ul.nav.navbar-nav.navbar-right.nav-sm
          %li{ :class => (controller_name == "activities" and action_name == "index") ? "active" : nil }
            %a{:href => root_url}
              %i.fa.fa-rss-square
              = t('.activity')
          %li{ :class => (controller_name == "events" and action_name == "search") ? "active" : nil }
            %a{:href => "/events/event_search"}
              %i.fa.fa-compass
              = t('.explore')

          %li{ :class => (controller_name == "users" and action_name == "search") ? "active" : nil }
            %a{:href => "/users/search"}
              %i.fa.fa-users
              = t('.people')

          - if user_signed_in?
            %li
              %a.events_new{:href => "#"}
                %i.fa.fa-plus-square
                = t('share')
            %li.dropdown
              %a.dropdown-toggle{:href => "#", "data-toggle" => "dropdown"}
                %i.fa.fa-user
                = current_user.complete_name
              %ul.dropdown-menu
                %li
                  %a{:href => user_path(current_user)}
                    %i.fa.fa-user
                    = t('.profile')
                %li
                  %a{:href => edit_user_registration_path(current_user)}
                    %i.fa.fa-cog
                    = t('.configuration')
                %li.divider
                %li
                  %a{:href => destroy_user_session_path, :data => {:method => :delete}}
                    %i.fa.fa-power-off
                    = t('.logout')
          - else
            %ul.nav.navbar-nav.navbar-right
              %li
                = link_to t('.sign_up'), new_user_registration_path
              %li
                = link_to t('.sign_in'), new_user_session_path

      .visible-xs
        %ul.navbar-xs.list-group.mtm
          %li.list-group-item{ :class => (controller_name == "activities" and action_name == "index") ? "active" : nil }
            %a{:href => root_url}
              .pull-left
                %i.fa.fa-rss-square.media-object
              .media-body
                = t('.activity')
          %li.list-group-item{ :class => (controller_name == "events" and action_name == "search") ? "active" : nil }
            %a{:href => "/events/event_search"}
              .pull-left
                %i.fa.fa-compass.media-object
              .media-body
                = t('.explore')
          %li.list-group-item{ :class => (controller_name == "users" and action_name == "search") ? "active" : nil }
            %a{:href => "/users/search"}
              .pull-left
                %i.fa.fa-users.media-object
              .media-body
                = t('.people')

          - if user_signed_in?
            %li.list-group-item
              %a.events_new{:href => "#"}
                .media
                  .pull-left
                    %i.fa.fa-plus-square.media-object
                  .media-body
                    = t('share')

        %ul.navbar-xs.list-group.mtm
          - if user_signed_in?
            %li.list-group-item
              %a{:href => user_path(current_user)}
                .media
                  .pull-left
                    %i.fa.fa-user.media-object
                  .media-body
                    = current_user.complete_name
            / %li.list-group-item
            /   %a{:href => edit_user_registration_path(current_user)}
            /     .media
            /       .pull-left
            /         %i.fa.fa-cog.media-object
            /       .media-body
            /         = t('.configuration')
            / %li.divider
            %li.list-group-item
              %a{:href => destroy_user_session_path, :data => {:method => :delete}}
                .media
                  .pull-left
                    %i.fa.fa-power-off.media-object
                  .media-body
                    = t('.logout')
          - else
            %li.list-group-item
              = link_to t('.sign_up'), new_user_registration_path
            %li.list-group-item
              = link_to t('.sign_in'), new_user_session_path
