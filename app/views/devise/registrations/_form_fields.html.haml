.row
  .col-xs-12
    .form-group{:class => resource.errors.has_key?(:complete_name)? 'has-error' : ''}
      .controls
        = form.text_field :complete_name, :value => omniauth_name, :id => 'complete_name', :placeholder => t('.complete_name'), :class => 'form-control'
.row
  .col-xs-12
    .form-group{:class => resource.errors.has_key?(:email)? 'has-error' : ''}
      .controls
        = form.email_field :email, :value => omniauth_email, :id => 'email', :placeholder => t('.email'), :class => 'form-control'
.row
  .col-xs-12
    .form-group{:class => resource.errors.has_key?(:password)? 'has-error' : ''}
      .controls
        = form.password_field :password, :id => 'password', :placeholder => t('.password'), :class => 'form-control'
.row
  .col-xs-12
    .form-group{:class => resource.errors.has_key?(:password_confirmation)? 'has-error' : ''}
      .controls
        = form.password_field :password_confirmation, :id => 'password_confirmation', :placeholder => t('.password_confirmation'), :class => 'form-control'