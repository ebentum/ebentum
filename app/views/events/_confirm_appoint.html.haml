.modal.fade{:id  => 'confirm_appoint'}
  .modal-dialog.card
    .modal-content
      .modal-header
        %button.close{"aria-hidden" => "true", "data-dismiss" => "modal", :type => "button"} ×
        = t('.confirm')
      .modal-body
        %p
          = t('.confirm_text')
        = render 'share'
      .card-footer
        %a.btn{:href => "#", "data-dismiss" => "modal"}
          = t('.close')
        %a.btn.btn-primary.btn-round-bottom-right{:href => "#", :id => 'confirm_appoint_btn', :data => {:eventid => @event.id}}
          = t('.confirm')