#event_main_picture_form
  = render 'pictures/main_picture_form', main_picture: @picture

= form_for @event, :html => { :multipart => true  } do |f|
  %input#event_main_picture_input{:name => "event[main_picture_id]", :type => "hidden"}
  .card-content
    = f.text_field :name, :placeholder => t(".name"), :class => "form-control"
    = f.text_area :description, :placeholder => t(".description_optional"), :class => "form-control", :rows => 4
  .pam
    .row
      .col-xs-6
        -# .input-prepend
        -#   %span.add-on
        -#     %i.icon-calendar
        -# .control-group
        -#   -#.fui-lock-16{:for => "event_start_date"}
        -# %label.control-label
        %i.fui-calendar-24
        = f.text_field :start_date, :placeholder => t(".date"), :data => { "date-format" => t("date.formats.bootstrap_datepicker")}
      .col-xs-6
        -# .input-prepend
        -#   %span.add-on
        -#     %i.icon-time
        %i.fui-time-24
        = f.text_field :start_time, :placeholder => t(".time")


    %i.fui-location-24
    %input#place_autocomplete{:placeholder => t(".place"), :type => "text", :name => "place_autocomplete"}
    = f.hidden_field :place
    = f.hidden_field :lat
    = f.hidden_field :lng

  #map_canvas

.row.card-footer
  .col-xs-3.col-sm-offset-6.footer-option.option-active
    .option-action
      %input#create_event_btn.btn.btn-primary{:name => "btn_login", :type => "submit", :value => t(".create")}
  .col-xs-3.footer-option
    .option-action
      %button.btn{"aria-hidden" => "true", "data-dismiss" => "modal"}
        = t(".cancel")