= form_for(@event) do |f|
  - if @event.errors.any?
    #error_explanation
      %h2
        = pluralize(@event.errors.count, "error")
        prohibited this event from being saved:
      %ul
        - @event.errors.full_messages.each do |msg|
          %li= msg

  .control-group.error
    = f.label :name, :class => 'control-label'
    .controls
      = f.text_field :name, :placeholder => "Name"
      %span.help-inline Please correct the error
  .control-group
    = f.label :description, :class => 'control-label'
    .controls
      = f.text_area :description, :placeholder => "Name"
  .control-group
    = f.label :start_datetime, :class => 'control-label'
    .controls
      = f.datetime_select :start_datetime, :placeholder => "Name"
  .control-group
    = f.label :place, :class => 'control-label'
    .controls
      = f.text_field :place, :placeholder => "Name"
  .control-group
    = f.label :user_id, :class => 'control-label'
    .controls
      = f.number_field :user_id, :placeholder => "Name"
  .control-group
    = f.label :active, :class => 'control-label'
    .controls
      = f.check_box :active, :placeholder => "Name"
  .control-group
    = f.label :photo, :class => 'control-label'
    .controls
      = f.text_field :photo, :placeholder => "Name"
